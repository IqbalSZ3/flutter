enum TransactionType { expense, income }

enum SavingsPeriod { weekly, monthly, yearly, custom }

enum InstallmentProvider { shopeePay, goPayLater, tiktokPayLater, other }

enum PaymentStatus { pending, paid, overdue }

enum BillingCycle { monthly, yearly }
